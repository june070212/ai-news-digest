# 02.02 — Convert Contoso.Orders.Jobs to SDK-style

Same treatment as 02.01, for the console job host. Still `net472` after this task.

**Exit criteria**: project builds on `net472`, produces the same executable, 0 new errors.
